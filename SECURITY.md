# 安全政策

## 支持的版本

| 版本 | 支持状态 |
|---|---|
| `main` | ✅ 持续维护与安全修复 |

## 报告方式

如果你发现安全漏洞，请通过 **GitHub Private Security Advisory** 私下报告：

> https://github.com/Oneideals/PhoneMic/security/advisories/new

请**不要**在非私有的 Issue 或评论中披露漏洞细节，以便我们在公开前完成修复。

若不便使用 Advisory，也可在本仓库新建 Issue 并明确标注为「安全相关（请勿公开细节）」，
维护者会与你私下沟通。

## 范围与说明

PhoneMic 是一个**纯本地**工具：

- 音频仅在你的 Mac 与同一局域网的手机之间传输，不连接任何外部/云端服务器。
- 仓库不包含任何密钥、令牌或个人数据。
- 涉及的能力面主要是：本地 HTTP 音频流、UDP 局域网发现、菜单栏辅助功能权限（监听右 ⌥ 键）。

### 威胁模型与访问控制

「纯局域网传输」描述的是**数据路径**，不等于**访问控制**。手机端的麦克风服务监听在
局域网接口上，所以同一 Wi-Fi 下的其他设备在网络层是够得着它的。为此手机端实现了配对码
（pairing token）机制：

- 手机首次启动时用 `SecureRandom` 生成配对码，存于 App 私有 `SharedPreferences`，
  在主界面显示。仓库与日志中都不含它。
- **无线连接必须携带配对码**：HTTP 走 `X-PhoneMic-Token` 请求头（或 `?t=`），
  UDP 推流注册走 `PHONEMIC_UDP_START <token>`。校验用 `MessageDigest.isEqual` 定长比较。
  不带或不匹配一律拒绝（HTTP 401 / UDP 静默丢弃）。
- **回环（USB / `adb forward`）连接免鉴权**，并可通过 `GET /token` 取回配对码。
  依据是：能在手机上执行 adb 转发的一方已经拥有设备本地访问权，鉴权不再增加安全边界。
  该端点对非回环来源返回 403。
- Mac 端把配对码存为 `.phonemic_token`（权限 `0600`，已在 `.gitignore` 中）。

配对码机制修复了两类具体问题：

1. **同网窃听**：此前任何同网设备 `curl http://手机IP:8080/` 即可拿到实时麦克风。
2. **UDP 放大反射**：此前任意一个 `PHONEMIC_UDP_START` 数据包就会让手机朝**包里的源地址**
   持续推 8 秒音频。源地址可伪造，因此既能把麦克风定向推给第三方，也能作为放大反射源
   （单个约 46 字节的请求可换来约 775KB 出向流量）。

### 仍需你自行判断的部分

- UDP 服务发现公告（`255.255.255.255:58080`）只含 IP 与端口，不含音频，因此不做鉴权。
  同网设备可以看到「这里有一台 PhoneMic」，但拿不到音频。
- 配对码是**局域网内的访问控制**，不是端到端加密。流量本身仍是明文；在完全不可信的
  网络（公共 Wi-Fi）里，请优先使用 USB 直连。
- 菜单栏应用会请求**辅助功能（Accessibility）**权限以监听右 ⌥ 键；这属于 macOS 标准权限，
  仅在本地生效，不会外传任何按键数据。

## 处理流程

1. 收到报告后，维护者会在合理时间内确认并评估影响。
2. 协商一个私下的修复与披露时间线。
3. 修复合并并发布后，在 Advisory 中公开致谢（如你愿意）。
