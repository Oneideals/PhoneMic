# Python
.venv/
__pycache__/
*.pyc

# Android 构建产物
android/.gradle/
android/build/
android/app/build/
android/local.properties
*.apk

# 运行时状态（含个人语音数据，严禁入库）
recordings/
.level
.phonemic_last_url
.phonemic_lock
gain_db
denoise
record
.ptt
icons/

.DS_Store
# 临时诊断日志（排查完毕后可连同 debuglog.py 一并移除）
.debug.log
.debug.log.1
sysinput
.prev_input
.menu_lock
.reconnect
auto_duck

# WorkBuddy 工作区数据
.workbuddy/

# 内部自动生成（baton 进度投影，非源码，随本地仓库重建）
PROGRESS.md
